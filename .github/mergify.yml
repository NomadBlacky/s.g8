pull_request_rules:
  - name: assign and label scala-steward's PRs
    conditions:
      - author=bot-nomadblacky
    actions:
      assign:
        users: [NomadBlacky]
      label:
        add: [dependency]
  - name: merge scala-steward's PRs
    conditions:
      - author=bot-nomadblacky
      - "status-success=test (8)"
      - "status-success=test (11)"
    actions:
      merge:
        method: squash
